# ss-actions-testing

## Purpose

This repository is a testing and sandbox environment used to exercise CI workflows such as linting, testing, and typechecking. It also serves as a proving ground for Aviator's stacked PR and merge queue tooling. The repository intentionally contains small sample files and fixtures rather than production code, so changes here are safe to make and are meant purely to trigger and validate automation.