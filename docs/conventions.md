# Frontend Conventions

Class names are composed with the `cn` utility. Template literals are not
used for conditional classes.

Every client component carries an explicit "use client" directive at the
top of the file.
